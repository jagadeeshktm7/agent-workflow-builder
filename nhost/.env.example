# Copy to nhost/.env (gitignored) and fill in:
# HASURA_GRAPHQL_ADMIN_SECRET=<your nhost admin secret>
# HASURA_GRAPHQL_ENDPOINT=https://<subdomain>.hasura.<region>.nhost.run
# (hasura-cli reads ./.env by default; endpoint in config.yaml wins if present)