version: 3
# Endpoint is public (not a secret) so this file is safe to commit.
endpoint: https://pgqeznozctfsgrgpgxwg.hasura.ap-south-1.nhost.run
metadata_directory: metadata
migrations_directory: migrations
seed_directory: seeds